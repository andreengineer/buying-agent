# PROMPT_ENTRY.md — LLM Context Loading Guide

**Read this file first.** It tells you exactly which files to read based on your context window budget.

---

## Token estimation notes

- All estimates are approximate (±20%). Measured via `wc -l` × 2.5 tokens/line avg.
- "Full" means the entire repo fits in ~8,000 tokens (well within any modern LLM context).
- The repo is intentionally small (~500KB). No chunking needed — you can load everything.

---

## Loading tiers

### 🟢 Tier 1: Minimal (4K context) — ~800 tokens

Read these three files to understand the system and who you are:

```
1. llms.txt          — One-line-per-file index (~50 tokens)
2. CONTEXT_INDEX.md  — Navigation map (~350 tokens)
3. PREMIUMUI-ACCESS.md — Your role and which files to read (~250 tokens)
```

✅ You now know: what this system is, who the actors are, your role.

### 🟡 Tier 2: Standard (8K context) — ~2,500 tokens

Add these files to Tier 1:

```
4. ALPHA-VALIDATION.md       — Stack validation protocol (~950 tokens)
5. BROWSER_LLM_PACKET.md     — Session init packet (~250 tokens)
6. architecture/ROUTING_RULES.md — Model routing (~250 tokens)
7. architecture/SPREAD_ARCHITECTURE.md — Spread orchestration (~450 tokens)
```

✅ You now know: validation protocol, session init procedure, model orchestration.

### 🟠 Tier 3: Full (16K+ context) — ~5,000 tokens

Add these files to Tier 2:

```
8.  goals/2026-06-03_alpha_launch_todo.md  — Active TODO (~200 tokens)
9.  architecture/ACCOUNTABILITY_GOVERNOR.md — Anti-escapism (~350 tokens)
10. architecture/ISAURA_SYSTEM_STATE.md     — System state (~250 tokens)
11. HERMES_README.md      — Hermes agent instructions (~500 tokens)
12. CLAW_README.md        — Claw execution instructions (~350 tokens)
13. CLOUD_POLICY.md       — Security policy (~250 tokens)
14. MARCELA_RUNBOOK.md    — Human relay runbook (~250 tokens)
```

✅ You now know: full system context, agent instructions, goals, policies.

### 🔴 Tier 4: Maximum (full repo) — ~8,000 tokens

Includes everything:

```
15. runbooks/RUNBOOK_browser_spread.md   — Browser spread procedure (~350 tokens)
16. manifests/spread_registry.json       — Model registry (~550 tokens)
17. manifests/context_manifest.json      — Integrity checksums (~1300 tokens)
18. scripts/*                            — Automation scripts (~400 tokens)
```

✅ You now know: everything in the canonical context repo.

---

## Reading order by role

### Claude UI (Co-chairman / Alpha auditor)
Minimal → ALPHA-VALIDATION.md → goals/ → architecture/ROUTING_RULES.md + ACCOUNTABILITY_GOVERNOR.md → board_reports/ latest

### GPT UI (Architecture / Data model)
Minimal → architecture/SPREAD_ARCHITECTURE.md → manifests/spread_registry.json → current goals

### Perplexity (Research)
Minimal → goals/ → architecture/ISAURA_SYSTEM_STATE.md → direct research

### Marcela (Human relay)
MARCELA_RUNBOOK.md only. Copy-paste exactly what you're told.

---

## When to use archive/

If a file references a past cycle or goal no longer in `goals/` or `board_reports/`, check:
- [`archive/ARCHIVE_INDEX.md`](archive/ARCHIVE_INDEX.md) — compressed summaries of old data
- Summary entries are ~50-100 tokens each — cheap to load

---

## Policy

- Do NOT request `.env` files, API keys, passwords, or local-only data.
- If asked for something not in this repo → ask Andre directly. Do not hallucinate.
- All output in `.md` format.
- Budget governor: flag if a proposal spends >40% of cycle budget in one step.

---

*Generated by Isaura. Part of the buying-agent context repo.*
